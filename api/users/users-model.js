const database = require('../../data/db-config.js');

const getAll = () => {
  return database('users')
} 
const getById = () => {
  
} 
const insert = () => {
  
} 
const update = () => {
  
} 

const destroy = () => {
  
} 

module.exports = {
    getAll,
    getById,
    insert,
    update,
    destroy
}